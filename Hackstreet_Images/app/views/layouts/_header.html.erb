<nav class="navbar navbar-inverse bs-dark">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "Hack Street", root_path, id: "placeholder_logo", class: "navbar-brand" %>
  </div>

  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li class=""><a href=<%= image_upload_path %>>Create Post <span class="sr-only">(current)</span></a></li>
    </ul>
    <%= form_with url: search_path, local: true, method: :get, html: { class: "navbar-form navbar-left form-horizontal" } do |form| %>
      <%= form.text_field :q, placeholder: "Search", data: { behavior: "autocomplete" }, class: "search-box" %>
    <% end %>
    <ul class="nav navbar-nav navbar-right">

    <% if user_signed_in? %>
      <li class=""><a href=<%= destroy_user_session_path %>>Sign out <span class="sr-only">(current)</span></a></li>
      <li class="">
        <a href=<%= show_path(current_user) %> class="navbar-img" role="button">
          <%= current_user.username %>
          <%= image_tag display_avatar(current_user)%>
        </a>
      </li>

    <% else %>
      <li class=""><a href=<%= new_user_session_path %>>Sign In <span class="sr-only">(current)</span></a></li>
      <li class=""><a href=<%= new_user_registration_path %>>Sign Up <span class="sr-only">(current)</span></a></li>
    <% end %>

    </ul>
  </div>
</nav>

<% if flash.any? %>  
  <div id="alert alert-info alert-dismissable">
    <a class="panel-close close" data-dismiss="alert">×</a> 
    <i class="fa fa-coffee"></i>
    <% flash.each do |key, message| %>      
      <div class="flash <%= key %>">
        <!--flash message-->
        <%= message %>     
      </div>    
    <% end %>  
  </div>
<% end %>