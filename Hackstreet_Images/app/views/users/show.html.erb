<div class="container-fluid">
  <div class="profile-row">
    <div class="col-xs-2 col-xs-push-2 img">
      <%= image_tag display_avatar(@user)%>
    </div>
    <div class="col-xs-6 col-xs-push-2">
      <div class="row">
        <div class="col-xs-12 about">
          <p>
            <span style="font-size:36px">
              <%= "@#{@user.username}" %><br>
            </span>
            <strong>Posts:</strong>
            <%= @user.images.count %>
            <a href=<%= "#" %>>
              <strong>Followers:</strong>
              <%= 123 %>
            </a>
            <a href=<%= "#" %>>
              <strong>Following:</strong>
              <%= 123 %><br>
            </a>
            <strong><%= "#{@user.full_name}" %></strong>
            <%= "#{@user.bio}" %>
          </p>

          <% if user_signed_in? %>
            <div class="row">
              <% if @user == current_user %>
                <%= link_to 'New Post', image_upload_path %> |
                <%= link_to 'Edit Info', edit_user_registration_path %> |
                <%= link_to 'Delete Account', registration_path(@user), data: { confirm: "Are you sure?" }, method: :delete %>
              <% else %>
                <%= link_to 'Follow', '#' %>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<hr>

<% if @user == current_user %>
  <%= "User has no posts!" %>
<% end %>

<%= render :partial => 'shared/images_grid', :locals => { image_list: @user.images, with_author: false } %>